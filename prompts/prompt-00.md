# Prompt 0

**Usuario:** Arrancamos el proyecto RIA, todavía no definimos el tema pero quiero dejar armada la estructura base con react, bootstrap, docker compose y la carpeta del proyecto lista para desarrollar.

**Respuesta:** Se creó el proyecto con Vite + React + TypeScript, se instaló Bootstrap como dependencia, se armó `docker-compose.yml` con un servicio para el frontend y otro para desarrollo, y se definió la estructura de carpetas (`src/components`, `src/pages`, `src/api`, `src/hooks`, `src/utils`). Todo listo para empezar a codear.
