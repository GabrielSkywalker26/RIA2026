# Prompt 2

**Usuario:** Los tests me dan error, no encuentra el módulo. Cómo configuro Vitest para que funcione con React?

**Respuesta:** Se instaló Vitest + React Testing Library + jsdom, se configuró en `vite.config.ts` con environment `jsdom` y setup files para los test-utils con MemoryRouter.
